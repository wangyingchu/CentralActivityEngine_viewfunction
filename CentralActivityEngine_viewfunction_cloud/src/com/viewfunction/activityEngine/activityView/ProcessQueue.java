package com.viewfunction.activityEngine.activityView;

public interface ProcessQueue extends Queue{
	/*Users can define exposed fields and indexes for two types of queues in 
	 document-centric workflows: process and user-centric. A process queue holds objects to be processed by an external workflow program. 
	 A user-centric queue is similar to the inbox for a user, in that the user's work resides in it.
	 */
}
